# Production Deployment Checklist

This document outlines the exact steps to build and deploy the Neighbors Shelter Management System to production.

## Prerequisites

- Node.js and pnpm installed
- dfx (Internet Computer SDK) installed and configured
- Access to the production canister IDs
- Backend canister already deployed with latest Wasm

## Build Steps

### 1. Backend Deployment

Ensure the backend canister is deployed first:

